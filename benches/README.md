# Benchmarks

Workspace-level benchmark artifacts and planning docs live here.

Implemented benchmark entrypoints currently live in crate-local `benches/` directories (e.g. `crates/fastchat-core/benches/filter_engine.rs`).
